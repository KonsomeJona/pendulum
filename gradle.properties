android.useAndroidX=true
android.nonTransitiveRClass=true

org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

kotlin.code.style=official

# Regle globale : sorties de build sur le disque D, jamais sur C ni E.
# Consomme par build.gradle.kts (racine). Chemin Windows car le build est lance via cmd.exe.
pendulum.buildRoot=D:/gradle-builds/pendulum
