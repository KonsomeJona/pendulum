<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ extraction rules (`android:dataExtractionRules`).

  WHY THIS FILE EXISTS, and why `allowBackup="false"` is not enough.
  `allowBackup="false"` covers cloud backup only. **Device-to-device transfer** (D2D, migration to
  a new phone) is a separate channel, governed by the `<device-transfer>` block below, and it
  ignores `allowBackup`. Without this file the Room database — which holds a complete night of
  sleep, medication doses and questionnaire answers — leaves for the manufacturer's infrastructure
  at the first change of phone.

  We exclude the root on both sides: nothing from this application must leave the device other
  than through the user's explicit export (`export/`, via SAF).
-->
<data-extraction-rules>
    <cloud-backup disableIfNoEncryptionCapabilities="true">
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
